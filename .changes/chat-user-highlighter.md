---
bump: minor
---
Chat: a user message is a strip of label-maker tape instead of a box: bold monospace letters embossed on a run of the primary colour, in the page colour, the run following the lines with the material layer's edge bands, right-aligned as before. The send control is a small round solid key inside the field's end, pinned to the bottom so it stays put as the field grows.
