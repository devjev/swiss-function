---
bump: patch
---
Explorer: reserve the vertical scrollbar gutter (scrollbar-gutter: stable) on the tree-grid viewport, matching the DataTable fix, so the sticky header and body columns stay aligned when a classic scrollbar appears (issue #70).
