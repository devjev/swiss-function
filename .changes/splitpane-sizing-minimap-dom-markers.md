---
bump: minor
---
SplitPane: controlled `size`, percentage sizes (`"30%"`) that keep their share of the container, a px panel clamped to a container that shrinks below it, `minMainSize`, and `onSizeChange(px, fraction)`. Minimap: `useMinimapMarkers(rootRef, sources)` measures markers from the DOM by selector (headings as labels, elements as blocks) and re-measures on resize and mutation; `measureMinimapMarkers` is the one-shot behind it.
