---
bump: minor
---
Button: a solid build. `build="solid"` is the machined front-panel key built like Knob: a cap over a visible side wall set away from the light by the key's height, the cast on the wall, an engraved label, and a press that travels the full height onto the wall (`aria-pressed="true"` latches it). Opt-in `round` makes a power-button circle (its wall a bevel ring, the label centred on its own ink), `finish` picks brushed or plain. A latched sheet key (`aria-pressed="true"`) sits sunk in place, the groove and no travel; a latched ghost key also takes its hover face.
