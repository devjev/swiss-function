---
bump: minor
---
Button: a solid build.  is the machined front-panel key built like Knob: a cap over a visible side wall set away from the light by the key's height, the cast on the wall, an engraved label, and a press that travels the full height onto the wall ( latches it). Opt-in  mounts it in a recessed cut-out,  makes a power-button circle on either build,  picks brushed or plain. A latched sheet key (`aria-pressed="true"`) sits sunk in place, the groove and no travel; a latched ghost key also takes its hover face.
