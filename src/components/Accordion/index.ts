// INTERNAL ONLY — not exported from the package (no barrel export, no
// componentNames entry, no package.json exports subpath). Reflow's narrow
// "accordion" collapse mode is the sole consumer.
export { Accordion } from "./Accordion";
